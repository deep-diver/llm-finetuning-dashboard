# Raw Data Directory

This directory holds raw source files fetched directly from external repositories (e.g. HuggingFace datasets).

Files placed here should be treated as **immutable baseline inputs** for the data preparation pipeline.
