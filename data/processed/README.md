# Processed Data Directory

This directory holds the training, validation, and test splits formatted in consistent formats (e.g. JSONL) and cleaned by the dataset preparation script.

## Core Splits:
* `train.jsonl`
* `valid.jsonl`
* `test.jsonl`
