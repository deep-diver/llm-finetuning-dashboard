# Runs Output Directory

This directory stores execution artifacts isolated by experiment identifier:
`runs/<run_id>/`

Each folder holds log files, schemas, and checkpoint manifests generated during the run.
